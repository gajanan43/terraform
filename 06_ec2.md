# launching ec2:

1) terraform.tf
2) providers.tf
3) ec2.tf

- all above files in the same folder

```
ec2.tf

# key pair
resource aws_key_pair my_key{
  key_name="terra-key-ec2"
  public_key=("terra-key-ec2.pub")
}

# VPC & Security Groups

```
- key pair -> ssh-keygen
